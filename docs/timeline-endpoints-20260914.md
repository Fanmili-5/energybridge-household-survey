# 运行色条直接标注起止时间

取代上一版图下时间表：每个运行时段的开始、结束时间常显在色条端点，并以短引线连接到对应坐标。次日端点保留“次日”前缀。

两份安排各保持一条水平轨道；密集时间标签上下错开，色条不因标签碰撞而换行或改变横坐标。相邻时段的同一个交界时间只标一次。持续充电中的功率变化不额外重复标“开始/结束”，原始功率色条和点击详情仍保留。温度设定变化保留分段标记。

手机 390px、桌面 1365px 检查了端点标注、横向无溢出、标签无重叠、运行条保持水平、完整与放大模式切换。Node 回归覆盖跨天排序、端点边界、密集标签排布及源数据不变。此次仅改变呈现，未运行模型或 EP，未修改仿真结果与费用。

已部署 UI v6.14。线上静态文件与本地一致，网站和计算服务发布哈希一致；25 项 HTTP / 权限 / 验证码拒绝检查通过，现有 3 个任务、21 份文档、2 份家庭提交内容不变。线上页面确认端点标签已显示、图下时间表已移除。
