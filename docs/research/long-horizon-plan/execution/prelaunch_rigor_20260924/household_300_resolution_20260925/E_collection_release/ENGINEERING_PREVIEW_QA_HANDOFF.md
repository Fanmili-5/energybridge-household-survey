# 角色页工程预览自查与后续试填交接

日期：2026-09-26。此记录只描述已验收模拟案例的工程预览；F 当前门为 `ROLE_TECHNICAL_PREVIEW_APPROVED`、`engineering_only=true`、`human_collection_approved=false`。真人参与、真人反馈和严格训练导出当前均为 0。

## 本次自查

- 本机入口：`http://127.0.0.1:8767/roles`；服务使用 `/private/tmp/eb-role-reviewed-preview-20260926` 作为隔离反馈目录，关闭规划与模型调用。当前会话进度会保留在该目录中；这只是本机临时服务，不能当作永久部署承诺。
- `/roles` 返回 200；`/api/roles/session` 返回 `ready=true`、`collection_mode=engineering_preview`；合住角色 `cityrole-0004` 的画像及第 1 日天气可读取。浏览器已检查 490×695 宽度：页首可见城市和模拟夏季第 1 日天气，住宅摘要包含成员年龄、设备、收入情境与偏好条件；合住有独立的私有房间/共用空间示意。
- 用 G 已验收的 3000 日案例实际走过第 1 日确认、第 2 日左右方案及工程回答。受控设备电量按 kWh 展示，室温按本户所有房间与 24 小时等权平均说明；第 1 日与等效日不索取偏好。工程回答来源为 `synthetic_engineering_test`，严格真人训练导出 0 行。`BENCHMARK_EXPORT_SAMPLE_EMPTY_HUMAN.json` 保存一个隔离工程检查例子；其中 1 条审计事件来自一次性本机测试目录，并非真人样本。
- 旧问卷 `/`、`/api/session` 和 `/api/households` 已做实际 HTTP 回归。部署组合另经临时服务验证：原问卷 `--human-pilot` 时仍报告 `human_pilot`，同一服务中的角色页默认仍为 `engineering_preview` 且技术门有效；单独请求角色真人模式被当前技术门拒绝。
- 接口测试 10 项、离线合同测试 8 项通过；`roles.js` 语法、Python 编译及差异空白检查通过。F 的模拟投影门审查与这些工程测试分别记录；这些结果不证明真人理解、主观舒适、家庭实测电量或电网收益。

## 真人认知试填前最少步骤

1. 先让 F 对当前页面、案例指纹和部署配置完成交付复核；另备适用的真人知情同意文本，并获得单独的真人采集批准。当前技术门不能用于真人收集。
2. 用未参与构建的试填者核对角色事实能否理解：住户与合住边界、设备操作范围、模拟天气日期、全天全房间平均温度、受控设备代理电量、左右盲序及“回答不改变后续轨迹”。记录误解和原话，不把试填当正式偏好标签。
3. 依次检查第 1 日确认、有效对照的选择/理由、等效日不索取偏好、评分可留空、“无法判断”、刷新后继续、改答与撤回。核对反馈来源、同意版本及导出隔离；在 F 真人门签发前只用工程会话。

本记录不改动已锁定的 A/B/C/D/G/F 输入、G pending 文件或 CSV/manifest 状态字段，也不触发 GitHub 推送。
