# 问卷说明的可读版图表

用于现有 Notion 页面：[问卷设计与数据构造完整链路](https://app.notion.com/p/3db06135328a81e683dbc035015aae3e)。

- `field-introduction.md`：一道题一行的字段说明，覆盖66个顶层字段；成员集合展开为10个子字段后共75行。正文使用缩写，完整原题以问卷代码表为准。
- `flow.svg` / `flow.png`：明确区分参与者、EB规划和EP计算；PNG为1480×2080，不依赖Mermaid在Notion内渲染。
- 页面截图为当前UI的局部，单图展示；家庭答案和反馈均为工程测试资料。结果读取上一轮实际EP测试案例，没有重新调用付费模型。
- 完整数据附件仍在原Notion页面，既有JSON和EP产物未改动。

校验：对照代码表逐字段核查；本地正文宽度预览；检查图片文件；更新后回读Notion，确认四个主章节、六张单列图片、三个完整数据附件，且无多列截图或Mermaid代码块。

浏览器没有Notion登录态，原生Notion应用的屏幕权限也未开放。因此本次确认了连接器保存结果和本地素材可读性，没有将其声称为登录后Notion客户端的视觉验收。
