# 验证记录

历史测试报告记录各次改动的检查结果，不能作为当前版本全部通过的证明。报告索引见[文档目录](README.md)。

## 2026-09-15 案例反馈与失败记录

- 新增选填昵称、案例编号复制、问题报告、管理员核查和诊断导出；沿用家庭问卷与案例关联，不更改 EB 提示词或真人评价标签。
- 60 项相关后端测试通过，涵盖报告权限、幂等重试、50 个并发重复请求、重启持久化、失败轨迹备份、队列和既有问卷契约；随后补充检查报告是否读取实际执行阶段。
- 隔离浏览器测试通过：390px 手机页面、昵称草稿恢复、编号完整显示、保存后断网再重试不重复入库、管理员筛选/核查/下载、刷新恢复。无浏览器脚本异常。
- 诊断导出会列出缺失阶段和附件限制，不声称远端尚未返回的轨迹已收齐。没有执行付费模型调用或新 EP 仿真，也未重跑整个规划测试集；前次记录的 6 项规划测试失败仍需单独处理。
- 线上代码更新至 `2331026` 后，计算端发布哈希校验通过，网页与计算服务均正常启动。云端隔离目录的 8 项新增后端测试通过；公开页面返回 200、未授权报告管理接口返回 403、管理员页面要求登录。生产库原有 4 个成功案例和 1 个超时案例保留，未写入测试报告；数据库检查为 `ok`。

## 2026-09-15 时间输入改动

提交 `3dc9803` 将洗衣机、洗碗机和烘干机改为先填时间窗口、运行时长，再选平时开始时间。

- 14 项相关后端测试通过，包含非法时间拒绝保存和跨午夜任务。
- 线上页面验证：08:00—22:00、运行 90 分钟时，开始时间只能选到 20:30。
- 网站与计算端发布哈希一致，私有计算接口检查通过。
- 本轮未调用付费模型，没有重新运行真实模型的完整流程。

同轮全量测试有 6 项规划／回退相关失败，尚未完成原因确认。相关功能测试通过不等于全量回归通过。

## 发布检查

部署前检查队列和计算端在途任务，备份数据；部署后核对两端版本、计算连接和数据库备份。具体步骤见[发布检查](../deploy/RELEASE-CHECK.md)。

涉及规划、提示词或执行逻辑的修改，还需要在隔离数据目录验证真实模型与 EP 的完整流程。固定模型回复的测试可检查工程流程，不能证明真实模型规划质量或问卷分发时的等待时间。
