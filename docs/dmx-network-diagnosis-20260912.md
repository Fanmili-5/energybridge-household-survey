# DMX 国内站连接诊断

检查时间：2026-09-12 00:38–00:40（UTC+8）。仅网络与只读接口检查，未发起模型生成。

## 结论

阿里云香港实例到 `www.dmxapi.cn`（`110.42.10.198`）的 TCP 连接未完成。不能据此确定是对端防护还是中间线路/回程故障。未发现实例操作系统防火墙阻断或默认路由异常。云控制台安全组/网络 ACL 尚未直接读取。

## 已验证证据

- 源公网 IP：`47.76.205.32`。
- 本地使用用户提供的国内站令牌 GET `/v1/models` 返回 200（0.72 秒），列表包含配置模型 `gpt-4o-mini`。不包含任何令牌内容。
- 云端 DNS 解析 `www.dmxapi.cn` 为 `110.42.10.198`；`/etc/hosts` 无 DMX 覆盖。
- `iptables -S` 的 INPUT/FORWARD/OUTPUT 默认均为 ACCEPT，无附加规则；nftables 规则集为空。
- 默认路由经 `172.19.127.253` 从 `eth0` 发出；策略路由为标准 local/main/default。
- 云端到 `www.aliyun.com:443`、`www.dmxapi.com:443`、`dmxapi.cn:443` 均能建立 TCP 连接。
- 云端到 `www.dmxapi.cn:80`、`:443` 及直接到 `110.42.10.198:443` 均超时。
- 仅针对目标 IP/443 的抓包显示 SYN 于 00:39:12、00:39:13、00:39:15 发出/重传，无捕获到该连接的 SYN-ACK 或 RST；抓包无内核丢包。
- TCP MTR 能收到多跳路由回应，最远有第 9 跳 `221.183.89.178` 回应，未获得目标握手。中间节点不回应 TTL 探测不能等同于业务流量丢包，不能据此认定故障节点。

## 建议服务商核查

请 DMX 检查此时间段源 IP `47.76.205.32` 到 `110.42.10.198:443` 的连接及防护日志，确认是否存在源 IP 拦截及回程问题。如有支持国内站令牌的备用入口，请提供官方地址。必要时向阿里云提交相同的源/目标 IP 与时间，排查目的地址特定的出站路径。

国内站、国际站地址不能在未确认令牌兼容性时直接互换。本次未修改防火墙、域名或生产 API 配置。
