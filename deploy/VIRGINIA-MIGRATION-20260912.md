# 问卷入口迁移到弗吉尼亚实例

2026-09-12 已完成迁移。新入口：**https://47.85.194.154/**；管理员入口：**https://47.85.194.154/admin**。普通账号和管理员密码沿用现有配置。旧入口 `https://47.76.205.32/` 对页面访问返回跳转；旧 POST 返回迁移提示，避免继续写入旧库。

## 运行位置与配置

| 部分 | 迁移后位置或配置 |
|---|---|
| 阿里云实例 | `i-0xi7wcpbicmld3a2k9f0`，Ubuntu 22.04，2 vCPU / 2 GiB |
| 网站、登录、队列、问卷与评分数据库 | `47.85.194.154` |
| 网站代码 | `/opt/energybridge/compute-v32-20260912` |
| 网站数据 | `/var/lib/energybridge/demo_20260911/jobs` |
| EB / EnergyPlus | 学校 `hku_user3@100.116.9.76`，现有计算服务 |
| 计算通道 | 新云端 `127.0.0.1:18768` → Mac `127.0.0.1:18769` → 学校 `127.0.0.1:18768` |
| 模型通道 | 学校 `127.0.0.1:18080` → Mac `127.0.0.1:18081` → 现有 DMX 站点 |
| 网站 worker / 在途上限 | 16 / 64；按新实例内存从旧网站的 32 worker 调低 |
| 学校任务 / EP / API 槽数 | 32 / 32 / 4，保持原配置 |
| 普通账号生成额度 | 每会话 2 次，UTC 每日合计 50 个新任务 |
| 采集模式 | `engineering`，规划开启，未改变真人标签准入方式 |

Mac 的 `org.energybridge.compute-tunnel` 与 `org.energybridge.api-tunnel` 两项 LaunchAgent 均已切换到新实例并验证运行。它们会在登录后启动、断线后重连；**新计算仍需要 Mac 保持开机、联网且不休眠**。旧实例不再承担问卷后端或 API 转发目标，但暂留 HTTPS 跳转与原数据。

新实例 SSH 使用 `/Users/fanmili/Downloads/MacBook.pem`；私钥权限为 0600，不进入仓库。内部服务端口 8767、18768、18080 均只监听回环地址。网站仅通过 Nginx 的 80 / 443 对外提供入口。

## 数据与版本校验

迁移前先拒绝旧入口的新写请求，确认没有排队或运行任务，再停止旧后端并制作最终一致性备份及完整目录归档。

- 一份家庭资料、两个历史任务（一个完成、一个超时）、13 份数据库文档。
- 三张 SQLite 表的内容哈希在迁移前后完全一致，完整性检查为 `ok`。
- 完整数据目录的 157 个文件逐个校验通过，包含完成与失败任务的历史痕迹。
- 网站和学校计算端的发布哈希一致：`743a931b0a00b900ffd3279cac7684b13cd2a3b27e5acdf0bc82dc1e768deb72`。492 个固定上游文件通过校验。
- 运行版本为 `eb.paired_ep.v3.2`。本次使用已部署版本，未把本地其他尚未发布的改动混入迁移。

旧实例最终备份位于 `/var/backups/energybridge/migration-virginia-20260912/final-cutover`。新实例迁移材料位于 `/root/energybridge-migration-20260912`。本地备份、逐项校验、切换脚本和测试报告位于 Git 忽略目录 `artifacts/migration_20260912_virginia/`，目录权限 0700。真实问卷、密钥、密码文件和归档不提交 Git。

## 验证结果

- 新 IP 的受信任 HTTPS 证书签发成功；普通与管理员登录、会话 API、管理员隔离均通过 HTTP 验证。
- 未登录或错误密码访问为 401，普通账号访问管理员页面为 403，敏感路径为 404，旧生成接口保持关闭。
- 独立工程数据库通过“新云队列 → 学校原生配对 EP → 结果哈希校验 → 新云 SQLite”测试，约 9.90 秒完成。模型使用固定替身，没有付费模型调用或真人标签；这不是实际模型规划延迟测试。
- 切换后的学校模型列表认证查询返回 200，配置的模型可用。原模型地址与密钥未改变。
- 小时备份 timer 已启用，实际执行成功，SQLite 完整性通过。
- 新 IP 使用短期证书，已启用每天两次的续期检查与 Nginx reload 钩子；续期演练及钩子验证通过。
- 网站自启动开启，旧后端及旧库定时备份停用，避免两处继续收集。旧实例未释放、未删除。

## 运维与回滚

网站服务为 `energybridge.service`，直接使用新主服务配置；旧迁移暂存的 `90-migration-staging.conf` 已移除。备份 timer 为 `energybridge-backup.timer`，证书 timer 为 `energybridge-cert-renew.timer`。

回滚前必须先冻结新入口写入并备份新库；若迁移后已有新提交，不能用旧实例数据库覆盖它。需要同时恢复网站源地址、Nginx、数据和 Mac 隧道配置，且保留同一发布版本。两个 Mac LaunchAgent 的切换前后副本保存在本地迁移目录中。

浏览器会话 cookie 绑定原 IP，换地址后需要重新登录；历史数据库记录仍完整保留，不自动把旧会话所有权开放给其他浏览器。
