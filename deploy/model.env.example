# Copy manually to /etc/energybridge/model.env; never commit the populated file.
# Planning is disabled independently by EB_PLANNING_ENABLED=0 in service.env.
USE_LLM=false
LLM_PROVIDER=openai_compatible
LLM_BASE_URL=
LLM_MODEL=
LLM_API_KEY=
